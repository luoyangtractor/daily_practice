#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>

using namespace std;
/*
long digui(int n, long w, vector<long> temp, long v_now, long )
{
	int size = temp.size();
	for (int i = 0; i < size; i++)
	{

	}
}

int main() {
	int n,
	long w;
	while (cin >> n >> w)//注意while处理多个case
	{
		vector<long> temp;
		for (int i = 0; i < n; i++)
		{
			long t;
			cin >> t;
			temp.push_back(t);
		}
		long ans = 0;

		

	}
	return 0;
}

*/