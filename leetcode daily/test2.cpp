#include <iostream>
#include <fstream>
#include <string>
#include <vector>
/*
using namespace std;

vector<string> readin(const string &filename);

int main()
{
	readin("C:\\Users\\wzh\\Desktop\\page 312.txt");
	return 0;
}

vector<string> readin(const string &filename)
{
	vector<string> ret;
	string temp;
	ifstream in;
	in.open(filename);
	if (in.is_open())
	{
		while (in)
		{
			getline(in, temp);
			ret.push_back(temp);
			cout << "ttt   " << temp << endl;
		}
	}
	else
	{
		cout << "fffffff" << endl;
	}
	return ret;
}
*/
