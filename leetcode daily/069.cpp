#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

/*
class Solution {
public:
	int mySqrt(int x)
	{
		long temp = 0, i = 0;
		while (1)
		{
			temp = i*i;
			if (temp == x) return i;
			if (temp > x)return (i - 1);
			if (temp*temp < x &&temp != 1 && temp != 0)i = temp;
			else i++;

		}
	}
};

*/