#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <fstream>
#include <string>

using namespace std;
/*

#define height 100
#define width 70

void generate(char* img, const string &filename)
{
	ofstream fout;
	fout.open(filename);
	int l = (width * 3 + 3) / 4 * 4;
	int bmi[] = { l*height + 54, 0, 54, 40, width, height, 1 | 3 * 8 << 16, 0, l*height, 0, 0, 100, 0 };
	fout<<"BM";
	fout << bmi << img;
	fout.close();
}

int main()
{
	cout << "adsasa" << endl;
	int a = -1;
	unsigned int b = 1;
	cout << a*b << endl;
	cin >> a;
	return 0;
}
*/

