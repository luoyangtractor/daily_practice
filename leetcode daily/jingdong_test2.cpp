#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>

using namespace std;
/*
bool judge(string s)
{
	for (int i = 1; i < s.length() / 2 - 1; i++)
	{
		if (s[i] != s[s.length() - 1]) return false;
	}
	return true;
}

int remove_n(string s, vector<vector<int>> matrix, int n)
{
	int count = 0;
	
}
int main() 
{
	string s;
	cin >> s;
	if (s.empty()) return 0;
	int ans = 0;
	for (int i = 0; i < s.length(); i++)
	{
		vector<vector<int>> matrix;
		for (int j = 0; j < i; j++)
		{
			vector<int> t(s.length(), 0);
			matrix.push_back(t);
		}
		ans += remove_n(s, matrix, i);
	}
	cout << ans;
	return 0;

}

*/

